<% if @task %>
  <h2><%= @task['name'] %></h2>
  <p><%= @task['details'] %></p>
  <a class='button' href="/tasks/<%= @id %>/edit">Edit</a>
  <a class='button' href="/tasks/<%= @id %>/delete">Delete</a>
<% else %>
  <p>An item with that ID doesn't exist</p>
<% end %>